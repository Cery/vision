目标方案（升级后的方案二）

平台居中可见密码：在“运营管理”页 management.html 中新增管理员面板。管理员输入 ADMIN_KEY 后，加载所有需求的明文密码、哈希与开关状态，支持单条更新或批量应用。
权限密码统一授权：可为所有需求批量设置明文密码（自动生成并写入哈希），同时可切换“开放报价”开关以允许全网供应商无需密码直接报价。
分权逻辑清晰：
查看联系方式：仍需查看密码（更安全，且符合你的需求）；后续可添加“公开联系方式开关”以脱敏显示部分信息。
在线报价：当 AllowOpenQuotes=true 时无需密码即可提交；当 AllowOpenQuotes=false 且 REQUIRE_QUOTE_PASSWORD=true 时必须先验证密码。
管理风格与样式：管理页采用 Bootstrap 5 卡片、表格与表单，简洁商务风格。提供“管理员密钥登录区”“全局设置区”“逐条更新表格”“报价列表与状态管理”。
已实现（代码层面）

管理函数
/.netlify/functions/adminListRequirements：鉴权后返回需求列表，含 ViewPasswordPlain、ViewPasswordHash、AllowOpenQuotes 等管理字段。
/.netlify/functions/adminUpdateRequirement：鉴权后支持批量或单条更新明文密码（自动生成哈希）与“开放报价”开关。
用户函数联动
/.netlify/functions/submitQuote：当启用 REQUIRE_QUOTE_PASSWORD=true 时，且该需求的 AllowOpenQuotes=false，提交报价必须附带正确的查看密码；若 AllowOpenQuotes=true 则跳过密码校验。
前端联动
需求详情页：密码验证成功后，自动把密码写入隐藏字段 ViewPassword，解锁报价表单控件并允许提交。
管理页 static/management.html：新增管理员密钥登录、全局设置（批量应用）、逐条更新表格（明文密码、哈希截断、开关）与已有报价面板。
配置与文档
.env.example 增加 ADMIN_KEY 与 REQUIRE_QUOTE_PASSWORD 示例。
docs/Airtable配置步骤.md 增加新字段说明与管理后台使用方法。
Airtable 字段规范（新增/关键）

Requirements 表新增字段：
ViewPasswordPlain（单行文本）：早期阶段平台可见的明文密码；正式阶段建议移除或只在管理员页显示。
ViewPasswordHash（单行文本）：bcrypt 哈希，前端/函数用于校验。
AllowOpenQuotes（复选框）：开启后供应商可无需密码直接在线报价。
说明与建议：
明文仅管理员可见；对外页面与函数均使用哈希校验。
后续可增加 ContactMaskPattern 或 AllowPublicContact，用于在“开放报价”场景下仍脱敏展示联系方式。
外部配置指引（需要你在 Airtable/Netlify 操作）

Airtable
给 Requirements 表添加字段 ViewPasswordPlain、ViewPasswordHash、AllowOpenQuotes。
可批量导入 docs/requirements.csv 与 docs/quotes.csv，再手动建立 RequirementRef 关联。
Netlify（云端）
Site settings → Environment variables：设置
AIRTABLE_API_KEY、AIRTABLE_BASE_ID
ADMIN_KEY（例如：随机 32 位字符串）
REQUIRE_QUOTE_PASSWORD=true（启用报价密码校验逻辑）
部署后访问 https://<your-site>/management.html，用 ADMIN_KEY 加载管理面板。
本地开发（Netlify Dev）
复制 .env.example 为 .env，填入以上变量。
运行 netlify dev（当前预览为 http://localhost:8888/），管理页地址 http://localhost:8888/management.html。
操作流程（平台侧，早期阶段）

设置密码
管理页顶部输入 ADMIN_KEY 加载管理面板。
在“全局设置”中输入新密码，点击“应用到全部需求”，系统写入 ViewPasswordPlain 并自动生成 ViewPasswordHash。
如需单条调整，在表格中编辑明文密码或切换“开放报价”后点击“更新当前”。
授权与通知
将查看密码通过短信/邮件告知发布方与指定供应商。
若开启“全网可报价”，供应商无需密码即可提交在线报价；查看联系方式仍需要密码（更安全）。
审核与管理
在管理页右侧查看报价列表，支持状态更新（接洽中/已中标/未中标）与删除。
可导出 CSV 做线下归档或后续分析。
验证与测试

管理页：打开 http://localhost:8888/management.html 输入 ADMIN_KEY，确认能加载需求表格（含明文/哈希/开关）。
需求详情页：
未开启“开放报价”时，必须输入查看密码才能提交报价；
开启“开放报价”后，未输入密码也能提交报价，但联系方式仍需密码解锁。
函数日志：若管理员操作或提交报价失败，检查 Netlify Dev 控制台日志与 Airtable 字段名是否一致。
安全与演进建议

明文密码仅限早期阶段；正式阶段建议只保留哈希，管理员面板移除明文列，改用“一次性链接/令牌”。
增加操作审计（函数层记录 IP、时间、需求编号）便于风控。
可加通知函数（短信/邮件）自动化：如 notifyPublisher.js 在新报价或需求创建时触发。
执行计划

第 1 步：Airtable 增加字段 ViewPasswordPlain、ViewPasswordHash、AllowOpenQuotes，核对表名与字段一致。
第 2 步：Netlify/本地 .env 设置 AIRTABLE_API_KEY、AIRTABLE_BASE_ID、ADMIN_KEY、REQUIRE_QUOTE_PASSWORD=true。
第 3 步：打开 http://localhost:8888/management.html，用 ADMIN_KEY 加载管理面板，批量设置密码与开关。
第 4 步：在“需求市场”页实测查看密码与报价提交流程（含开启/关闭开放报价两种路径）。
第 5 步：如需上线，照上述环境变量在 Netlify 云端设置并重新部署。
需要我现在把“公开联系方式开关/脱敏显示规则”也加上吗？如果你确认执行以上方案，我就继续把该开关加入 Airtable 字段与前端逻辑（默认脱敏显示，解锁后显示完整）。

-----------------------------------------------------------------------

升级内容概览

- 管理后台页 management.html 增强：支持管理员密钥登录后查看所有需求的明文密码、哈希、开关状态，单条更新与批量应用。
- 前台需求详情页增强：未验证时按“公开联系方式（脱敏）”开关显示掩码信息；验证密码后显示完整联系方式。
- 开放报价联动：当 AllowOpenQuotes=true 时，供应商无需密码即可在线报价（联系方式仍需密码解锁）；当为 false 且 REQUIRE_QUOTE_PASSWORD=true 时必须先通过查看密码后才允许报价。
- 后端函数完善：
  - 新增 adminListRequirements 与 adminUpdateRequirement （管理员密钥鉴权、批量/单条更新明文密码/哈希/开关）。
  - 扩展 listRequirements 返回更多字段（联系方式、开关、参数等）。
  - 更新 submitQuote 支持跳过密码（当开启全网可报价）或要求密码校验。
- 文档与环境示例更新： .env.example 增加 ADMIN_KEY 、 REQUIRE_QUOTE_PASSWORD ， docs/Airtable配置步骤.md 增加新字段说明与管理后台用法。
前台行为说明

- 联系方式显示
  - 未验证：当 ContactPublic=true 时，显示脱敏信息（姓名首字母＋＊＊、电话前三后四、公司前两字＋省略号）。
  - 验证成功：显示完整姓名、电话、公司。
- 在线报价
  - 当 AllowOpenQuotes=true ：直接解锁报价表单（可提交），但联系方式仍需密码。
  - 当 AllowOpenQuotes=false 且 REQUIRE_QUOTE_PASSWORD=true ：需要验证密码后才能解锁报价表单。
管理后台使用

- 页面地址： /management.html
- 登录与加载
  - 输入管理员密钥（ ADMIN_KEY ）后点击“加载需求管理”，即可看到需求表格。
- 单条更新
  - 在对应行修改“明文密码”“开放报价”或“公开联系方式（脱敏）”，点击“更新当前”写入 Airtable：
    - 明文密码：后端自动生成并写入 ViewPasswordHash 。
    - 开放报价：写入 AllowOpenQuotes 。
    - 公开联系方式（脱敏）：写入 ContactPublic 。
- 批量应用
  - 顶部“全局设置”：可一次性为所有需求设置明文密码（并生成哈希）、开放报价开关、公开联系方式开关。
Airtable 字段（Requirements 表）

- 新增或使用的关键字段：
  - ViewPasswordPlain （单行文本）：早期阶段平台可见的明文（管理员页显示）。
  - ViewPasswordHash （单行文本）：bcrypt 哈希，用于校验。
  - AllowOpenQuotes （复选框）：是否允许全网供应商无需密码直接报价。
  - ContactPublic （复选框）：是否公开联系方式（脱敏显示）。
  - 其他字段： RequirementID 、 Title 、 PublicPreview 、 PrimaryCategory 、 SecondaryCategory 、 ContactName 、 ContactPhone 、 ContactCompany 、 BudgetRange 、 PublishedAt 、 Progress 、 Parameters 。
Netlify 环境变量（必配）

- AIRTABLE_API_KEY 、 AIRTABLE_BASE_ID
- ADMIN_KEY （用于调用管理员函数）
- REQUIRE_QUOTE_PASSWORD=true （启用报价提交密码校验；单条需求若 AllowOpenQuotes=true 则跳过）
- 可选自定义表名： AIRTABLE_REQUIREMENTS_TABLE 、 AIRTABLE_QUOTES_TABLE
本地开发与验证

- 复制 .env.example 为 .env ，填写上述变量。
- 启动本地： netlify dev （预览 http://localhost:8888/ ，管理页 http://localhost:8888/management.html ）
- 验证流程：
  - 管理页：用 ADMIN_KEY 加载并批量设置明文密码/开关；执行单条更新。
  - 前台页：选择需求 → 弹窗中查看联系方式脱敏展示；输入密码后解锁完整联系方式。
  - 开放报价：当某需求 AllowOpenQuotes=true 时，未验证也能填写并提交报价表单；当为 false 时必须先验证密码。
执行计划

- 第 1 步（Airtable）：为 Requirements 表新增 ViewPasswordPlain 、 ViewPasswordHash 、 AllowOpenQuotes 、 ContactPublic 字段，核对字段名一致。
- 第 2 步（Netlify/本地）：设置环境变量 AIRTABLE_API_KEY 、 AIRTABLE_BASE_ID 、 ADMIN_KEY 、 REQUIRE_QUOTE_PASSWORD （云端与本地 .env ）。
- 第 3 步（管理后台）：在 /management.html 登录并批量设置密码与开关；单条微调按需更新。
- 第 4 步（前台验证）：在“需求市场”页面查看脱敏展示；输入密码验证显示完整联系方式；按开启/关闭“开放报价”两种情况提交报价并观测函数行为。
- 第 5 步（上线）：在 Netlify 站点环境变量中配置同样变量并部署，云端函数与管理页照流程使用。
可选后续增强

- 定制化脱敏规则：增加 ContactMaskPattern 字段（例如电话掩码策略、姓名掩码长度），前端按模式渲染。
- 审计与通知：接入短信/邮件服务（如 Twilio、SendGrid/Mailgun），在“新需求/新报价/解锁查看”事件自动通知发布方；同时记录操作日志（IP、时间、需求编号）。
- 安全进阶：正式阶段去除明文显示，只保留哈希；改用一次性令牌或短期有效链接替代固定密码。
如果你确认上述执行计划，我将继续为管理页加“联系方式公开开关”的批量与单条操作验证，并按需补充自定义脱敏规则字段的前端支持与文档说明。

-------------------------------------------------------
- 运营流程（早期阶段）
  - 批量设置密码：在管理页“全局设置”输入明文密码，点击“应用到全部需求”（自动写入哈希）；或在某行单独设置。
  - 授权通知：将查看密码通过短信/邮件告知发布方与指定供应商；需要开放全网报价则开启 AllowOpenQuotes 。
  - 联系方式：开启 ContactPublic 后前台脱敏显示；输入密码解锁完整显示。
如何验证

- 管理页：输入 ADMIN_KEY → 加载列表 → 查看统计与表格 → 测试“仅开放报价”筛选与搜索。
- 前台页：打开任意需求详情 → 未验证时脱敏显示（若 ContactPublic=true ）→ 验证密码后显示完整联系方式。
- 报价提交流程：
  - 当 AllowOpenQuotes=true ：无需密码直接可提交报价；联系方式仍需要密码查看。
  - 当 AllowOpenQuotes=false ：需先验证密码，页面自动将密码注入隐藏字段并解锁报价表单，然后才能提交。
后续可选增强

- 列表筛选补充：增加“仅联系方式脱敏”筛选、分类多选。
- 通知自动化：新增函数集成短信/邮件服务，自动在“新需求/新报价/解锁查看”事件通知发布方。
- 安全与审计：移除明文列（正式阶段）、增加操作日志（IP、时间、需求ID）、一次性令牌替代固定密码。
如果你认可当前页面风格但希望进一步提升（配色、图标、侧边导航），我可以继续：

- 增加左侧固定侧栏与模块化导航（概览/需求管理/报价管理/系统设置）。
- 引入主题色变量与统一按钮样式，微调卡片间距与表格密度。
- 为管理页提供导出 Excel（XLSX）与筛选条件的持久化。